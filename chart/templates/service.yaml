apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsi-aura.fullname" . }}
  labels:
    {{- include "nsi-aura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nsi-aura.selectorLabels" . | nindent 4 }}
